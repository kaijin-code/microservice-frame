<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="app.storage.mapper.StorageMapper">

    <insert id="reduceStorage">
        insert into storage_tbl (commodity_code) values (#{commodityCode})
    </insert>
</mapper>